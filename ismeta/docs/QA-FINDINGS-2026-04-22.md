# QA Findings — 2026-04-22

**Тестер:** Андрей (PO). **Тех-лид:** Claude. **Стенд:** `localhost:3001` (main @ `fe854ec+` с E15.04 Вариант B).

**Цель сессии:** прогон второй Спецификации через полный цикл для валидации E15.04 на PDF другой структуры. Файл — `ismeta/tests/fixtures/golden/spec-aov.pdf` (2 страницы A3, 5072 симв text layer, rotation=0). Автоматики + Кабели + Лотки — иная структура колонок чем spec-ov2 (152 items).

**Формат:** замечания Андрея → корневые причины (R17–R22) → план фиксов (E15.05 две итерации).

---

## Результат прогона Recognition

- `pages_total=2, pages_processed=2, items_count=29, errors=0`
- **Все 29 позиций извлечены, все `quantity` корректны** — функциональный minimum пройден.
- LLM: 9 calls (параллельный batch), 8670 + 2642 tokens.

## Замечания Андрея (по скриншоту + DB state)

### #26 🟠 major — префикс порядкового номера в name

**Цитата:** «Если в первой Спецификации там были проектные обозначения систем типа "ВД1" и соединение их со столбцом Наименование через тире "-" — было полностью верно. Тут не так — соединение порядковых номеров с наименованием не нужно. Даже если это цифры вида "1.1" или "2.4" — соединять их с Наименованием не нужно.»

**Симптом в DB:** `name = "1.1 Комплект автоматизации…"`, `"1.2 Комплект…"`, …, `"2.1 Корпус…"`, `"3.1 Кабель…"`.

**Корневая причина:** LLM в промпте (правило 5 «Префикс-колонка») не различает алфавитно-цифровой код системы («ПВ-ИТП», «ВД1») от чистого порядкового номера («1.1», «2.4»). Склеивает всё подряд через `-` / пробел.

**Severity:** 🟠 major (косметически мешает, но всё редактируемо).

**Root cause:** **R21** — Numeric vs system code detection в промпте.

---

### #27 🔴 blocker — shift колонок для items 1-15 (model/brand/unit/qty)

**Цитата:** «Позиции с 1 по 10 — не определились единицы измерения. В Спецификации чётко прописано "шт.", в смету попало "1.0" на все десять позиций. Также на все 10 позиции неверно определена модель. Вместо модели взят столбец "Завод-изготовитель".»

**Симптом в DB (items 1-10):**
```
name = "1.1 Комплект автоматизации…"
unit = "1.0"              ← должно быть "шт."
quantity = 1.0000         ← случайно совпало
tech_specs.brand = "шт."  ← должно быть "ООО КОРФ"
tech_specs.model_name = "ООО КОРФ"  ← должно быть пусто
```

Для items 12-15 шифт повторяется (unit="255.0" вместо "м", brand="м" вместо "КАБЕЛЬ", model="КАБЕЛЬ\"" вместо пусто).

**Диагностика:** прогон `extract_structured_rows(page1)` для тех же row'ов **возвращает корректный маппинг**:
```json
{
  "cells": {
    "name": "1.1 Комплект автоматизации для приточной установки П1 в комплекте с П1",
    "brand": "ООО \"КОРФ\"",   // ← правильно
    "unit": "шт.",              // ← правильно
    "qty": "1,00",              // ← правильно
    "comments": "-учтено разделом ИОС4"
  }
}
```

→ **Bbox-парсер корректен, баг в LLM**. LLM «интеллектуально» переставляет поля когда видит отсутствующий `cells.model` — берёт `cells.brand` как `model_name`, `cells.unit` как `brand`, `cells.qty` как `unit`. Циркулярный shift.

Дополнительный фактор — LLM non-determinism: мой свежий curl-прогон вернул **корректный** JSON с `unit="шт."` и `model_name=""`, тогда как Андреев ранний прогон дал шифт. `temperature=0` не гарантирует детерминизм OpenAI.

**Severity:** 🔴 blocker (15 из 29 позиций с полностью неверными Ед.изм., Модель, Brand).

**Root cause:** **R19** — жёсткое правило в NORMALIZE_PROMPT: «не переставляй поля, копируй cells 1:1».

---

### #28 🔴 blocker — 3 раздела из 5 пропущены полностью

**Цитата:** «Полностью пропущены названия разделов — на первом листе это были "Оборудование автоматизации", "Щитовое оборудование", "Кабели и провода". На второй странице было два раздела "Электроустановочные изделия" и "Лотки" и оба проигнорированы.»

**Симптом в DB:** только 2 уникальных `section_name` — `"3. Кабели и провода"` и `"5. Лотки"`. Остальные 3 секции не созданы, items 1-10 приклеились к «3. Кабели и провода» (sticky с следующей страницы).

**Диагностика:** прогон `extract_structured_rows` показал:
- row 1 = `{"name": "1. Оборудование автоматизации"}`, **`is_section_heading: false`** ← regex не поймал.
- row 12 = `{"name": "2. Щитовое оборудование"}`, **`is_section_heading: false`** ← regex не поймал.

`_SECTION_RE` заточен на ОВиК: `Система|Клапаны|Противодымн|Общеобменн|Воздуховоды|Слаботочн|Отопление|Кондиционирован|Дымоудален|Приточная|Вытяжная`. Слов «Оборудование», «Щитовое», «Кабели», «Электроустановочные», «Лотки» там нет.

**Дополнительно:** префикс `N. ` сохранён в section_name («3. Кабели и провода» вместо «Кабели и провода»).

**Severity:** 🔴 blocker.

**Root cause:** **R17 расширение** — более общая эвристика section detection + очистка префикса.

---

### #29 🟠 major — штамп «Взаим. инв. № 5.6» попал в name (item 26)

**Симптом в DB:** `name = "Взаим.инв. № 5.6 Шпилька М8х1000"`. Штамп ЕСКД (служебная надпись) склеился с настоящим именем.

**Диагностика:** `_STAMP_EXACT` в `pdf_text.py` не содержит «Взаим.инв.» (есть «Взам. инв. №» и «Инв. №»). Формат с точкой после «Взаим» не ловится exact-match фильтром.

**Severity:** 🟠 major (одна позиция, но тип ошибки повторится).

**Root cause:** **R20** — расширить фильтр штампов.

---

### #30 🟠 major — multi-line name/model с неравными длинами (items 12-16, 17-20, 23-24)

**Цитаты:**
- «Пункты 12-16 — все ошибки одинаковые. Наименование в Спецификации расписано на трёх строках, а модель на двух строках. Система взяла только первые строки и для наименования и для Модели.»
- «Пункт 23 — также пропущена вторая строка наименования, наименование обрывается на "в комплекте с".»
- «Пункт 24 — аналогично.»

**Симптом в DB (item 12):** `name="…КГППнг(A)-HF 3х1,5"` — только первая строка из трёх. Полный текст должен включать продолжение из PDF.

**Диагностика:** в spec-ov2 multi-line работал, когда name занимал 2 строки одной и той же колонки. Тут — name 3 строки, model 2 строки. LLM не знает как их склеивать когда длины разные.

**Severity:** 🟠 major (обрывы 7+ позиций).

**Root cause:** **R18** — правило в промпте: «многострочные cells склеивай независимо, длины колонок могут не совпадать; границу row определяет `qty` + `unit` якорь».

---

### #31 🟠 major — «Код оборудования» берётся как model вместо склейки

**Цитаты:**
- «Поз. 11. Взяла из "Код оборудования" некое дополнение к модели и поставила это как Модель. Я бы сделал так — взял и модель и код модели — и сделал из этого модель.»
- «Поз. 17 "Короб с крышкой…" — модель "прилепилась" к названию, а в модель собственно попала из Кода оборудования цифра "1788". Правильнее конечно этот код было "прилепить" к модели.»
- Аналогично поз. 18-20 (диаметр 16/20/25 vs код 91916/91920/91925), 26-29.

**Симптом в DB (item 11):** `model = "TI5-10-N-040-030-020-66"` (это код, а Модель "TITAN 5 ЩМП-40.30.20" ушла в name вместе с «Корпус металлический…»). Должно быть: `name="Корпус металлический навесного типа, IP66"`, `model="TITAN 5 ЩМП-40.30.20 + TI5-10-N-040-030-020-66"` (или через `-`).

**Решение Андрея:** склеивать «Модель» + «Код оборудования» в **одно поле `model_name`** через разделитель.

**Severity:** 🟠 major.

**Root cause:** **R19** (вторая часть) — промпт-правило склейки модели и кода оборудования.

---

### #32 🟡 minor / фича — «Завод-изготовитель» как отдельная колонка

**Цитата:** «Вместо модели взят столбец "Завод-изготовитель". Замечу, что я считаю, что нам также нужен такой столбец.»

**Запрос:** новое поле `SpecItem.manufacturer: str` + UI-05 столбец «Завод» (после «Модель») + миграция `tech_specs.manufacturer` или отдельная колонка в `EstimateItem` (решение — в `tech_specs` без миграции, как `comments`).

Сейчас «Завод-изготовитель» мапится в `SpecItem.brand` — путает с производителем. Правильно: **brand — бренд оборудования** (Daikin, Корф); **manufacturer — завод-поставщик** (ООО «КОРФ», АО «ДКС»).

**Severity:** 🟡 фича-запрос, не баг.

**Root cause:** **R22** — расширение схемы `SpecItem` + UI.

---

### #33 🟡 minor — Примечание для item 11 пропущено

**Цитата:** «Пропущено для пункта 11 Примечание. Оно в Спецификации есть, в смете просто прочерк.»

**Диагностика:** примечание «Для установки частотных…» в `extract_structured_rows` попало в **отдельный row 13 с только `comments`** (не привязано к item 11). Промпт не знает что это продолжение соседнего item.

**Severity:** 🟡 minor (одна ячейка).

**Root cause:** **R18 / R19** — промпт должен «подклеивать» orphan-comments к предыдущему item если у него comments пусто.

---

### #34 🟢 позитив — все 29 items на месте, quantity корректные

**Цитата:** «НО! все 29 пунктов на месте! Ни одной ошибки в количестве.»

Большое улучшение по сравнению с E15.03 hybrid. Вариант B работает. **Не ломать** — это guardrail для E15.05.

---

## Сводка root causes

| Код | Описание | Закрывает | Priority |
|---|---|---|---|
| **R17 ext** | `_SECTION_RE` расширение + эвристика «только name непустой → кандидат на heading» + очистка префикса `N.` | #28 | 🔴 blocker |
| **R18** | Multi-line cells с **неравными** длинами, якорь row по `qty+unit` | #30, #33 | 🟠 major |
| **R19** | LLM column shift: жёсткое правило «копируй 1:1» + склейка «Модель»+«Код оборудования» через `-` | #27, #31 | 🔴 blocker |
| **R20** | Штамп «Взаим. инв. №» + альтернативные формы — расширить `_STAMP_EXACT` | #29 | 🟠 major |
| **R21** | Numeric pos (`\d+(?:\.\d+)*`) НЕ склеивать; alphanumeric — склеивать | #26 | 🟠 major |
| **R22** | Новое поле `SpecItem.manufacturer` + `tech_specs.manufacturer` + UI-05 столбец «Завод» | #32 | 🟡 фича |

---

## План E15.05 — две итерации

### Итерация 1 (blocker + обратная совместимость)

- **R17 ext + R19 + R20 + R21.**
- Только `NORMALIZE_PROMPT` + `_SECTION_RE` + `_STAMP_EXACT` + `_looks_like_section` heuristic.
- Без изменений схемы `SpecItem`.
- Dual-regression: `spec-ov2-152items.pdf` ≥145 items, `spec-aov.pdf` 29/29 items + 4 секций + корректный mapping полей.
- 1 день, IS-Петя.

### Итерация 2 (major + фича)

- **R18 + R22.**
- Multi-line unequal lengths (R18).
- Новое поле `SpecItem.manufacturer` + apply_parsed_items → `tech_specs.manufacturer` (R22).
- ТЗ UI-05 для Феди: столбец «Завод» в items-table.
- 1 день, IS-Петя + параллельно Федя.

### Решение Андрея — «лучшее враг хорошего»

Каждая итерация — отдельный PR с dual-regression. Не объединять.

---

## Итого

**Новых находок:** 9 (#26–#34).
**Severity:** 🔴 × 2 (R17 ext, R19), 🟠 × 4 (R18, R20, R21, один R19 частный), 🟡 × 2 (R22, R33), 🟢 × 1 (#34 позитив).

**`spec-aov.pdf` утверждён как второй golden fixture.**
