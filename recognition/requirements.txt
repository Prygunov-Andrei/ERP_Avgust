fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.2
httpx[http2]>=0.27
PyMuPDF>=1.24
# TD-17: IBM Docling — open-source table structure recognition
# (Apache 2.0, AAAI 2025, 97.9% cell accuracy via TableFormer).
# Используется как опциональный extract path при PDF_EXTRACT_VIA_DOCLING=true.
docling>=2.0
# TD-17 force_full_page_ocr (RapidOcrOptions) requires onnxruntime backend.
onnxruntime>=1.17

# Dev/test
pytest>=8.2
pytest-asyncio>=0.23
pytest-cov>=5.0
ruff>=0.4
mypy>=1.10
